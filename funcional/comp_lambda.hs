-- ! Comosição de Funções 
    -- * (func1 . func2) param = func1 (func2 param) 

-- ! Lambda (\)
    -- * \param -> 2*param

-- ! Aplicações Parciais
    -- * Omitir parametros -> func = mult 2 (==) func x = mult 2 x
    -- Quando esses params variam muito, para deixar mais compacto
    -- ? Isso tem a ver com associatividade

-- ? OBS
    -- As funcoes basicas de aritmetica podem ser tratadas como funcoes: 11 + 11 == (+) 11 11
    -- ? TUDO é FUNÇÃO
